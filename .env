# URL base da API (backend Carta Fácil).
# Em desenvolvimento local, use o endereço onde o Django está a correr.
# As variáveis têm de começar com VITE_ para serem expostas no frontend.
VITE_API_URL=http://localhost:8000/api
